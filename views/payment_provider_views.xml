<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="payment_provider_form" model="ir.ui.view">
        <field name="name">SEP Provider Form</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="provider_credentials" position="inside">
                <group invisible="code != 'sep'">
                    <field name="sep_terminal_id" required="code == 'sep' and state != 'disabled'"/>
                    <field name="sep_password" required="code == 'sep' and state != 'disabled'" password="True"/>
                </group>
            </group>
        </field>
    </record>
</odoo>